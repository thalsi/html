# 🧾 HTML Master-Level Course (Beginner to Advanced)

A complete and professional guide to mastering HTML, from fundamentals to real-world applications, accessibility, APIs, and final projects.

---

## 📚 Table of Contents

1. [Introduction to HTML](#1-introduction-to-html)  
2. [Basic HTML Structure](#2-basic-html-structure)  
3. [HTML Elements & Tags](#3-html-elements--tags)  
4. [Text Formatting & Typography](#4-text-formatting--typography)  
5. [Links & Anchors](#5-links--anchors)  
6. [Images & Graphics](#6-images--graphics)  
7. [Lists](#7-lists)  
8. [Tables](#8-tables)  
9. [Forms & Input Elements](#9-forms--input-elements)  
10. [Semantic HTML](#10-semantic-html)  
11. [Multimedia (Audio & Video)](#11-multimedia-audio--video)  
12. [HTML5 APIs](#12-html5-apis)  
13. [Meta Tags & SEO Basics](#13-meta-tags--seo-basics)  
14. [Accessibility (A11y) in HTML](#14-accessibility-a11y-in-html)  
15. [Global Attributes](#15-global-attributes)  
16. [Advanced HTML Concepts](#16-advanced-html-concepts)  
17. [Tools & Performance Optimization](#17-tools--performance-optimization)  
18. [Final Project: Build a Complete Website](#18-final-project-build-a-complete-website)

---

## 1. Introduction to HTML

- What is HTML?
- History of HTML (HTML1–HTML5)
- How HTML Works (Browser Rendering)
- Basic Tools (Text Editors & Browsers)
- HTML vs. CSS vs. JavaScript
- Inspecting HTML with DevTools
- Quirks Mode vs. Standards Mode

---

## 2. Basic HTML Structure

- `<!DOCTYPE html>` Declaration
- `<html>` Root Element
- `<head>` Section (Metadata)
- `<body>` Section (Visible Content)
- Comments (`<!-- Comment -->`)
- HTML Syntax Rules

---

## 3. HTML Elements & Tags

- Opening & Closing Tags
- Self-Closing Tags (`<img>`, `<br>`, `<hr>`)
- Block vs. Inline Elements
- Nesting Rules
- HTML Section Commenting
- Void Elements

---

## 4. Text Formatting & Typography

- Headings (`<h1>` to `<h6>`)
- Paragraphs (`<p>`)
- Bold (`<b>`, `<strong>`), Italic (`<i>`, `<em>`)
- Underline (`<u>`), Strikethrough (`<s>`, `<del>`)
- Superscript (`<sup>`), Subscript (`<sub>`)
- Preformatted Text (`<pre>`), Code (`<code>`)
- Quotations (`<blockquote>`, `<q>`, `<cite>`)
- Horizontal Rule (`<hr>`), Line Break (`<br>`)

---

## 5. Links & Anchors

- `<a>` Tag (Hyperlinks)
- Absolute vs. Relative URLs
- Linking to Sections (`#id`)
- Email (`mailto:`), Phone (`tel:`), Download Links
- Link Targets (`_blank`, `_self`, etc.)
- Button vs Link
- `<base>` Element

---

## 6. Images & Graphics

- `<img>` Tag (`src`, `alt`, `width`, `height`)
- Image Formats (JPG, PNG, SVG, WebP)
- Responsive Images (`srcset`, `sizes`)
- Lazy Loading (`loading="lazy"`)
- `<figure>`, `<figcaption>`
- Decorative vs Informative Images

---

## 7. Lists

- Ordered Lists (`<ol>`)
- Unordered Lists (`<ul>`)
- Definition Lists (`<dl>`, `<dt>`, `<dd>`)
- Nested Lists
- Styling Lists with CSS

---

## 8. Tables

- Basic Table Structure (`<table>`, `<tr>`, `<td>`, `<th>`)
- Table Sections (`<thead>`, `<tbody>`, `<tfoot>`)
- Merging Cells (`colspan`, `rowspan`)
- Table Captions (`<caption>`)
- Responsive Table Techniques
- Accessibility in Tables

---

## 9. Forms & Input Elements

- `<form>` Tag (`action`, `method`, `enctype`)
- Input Types (`text`, `password`, `email`, `number`, etc.)
- Textarea (`<textarea>`)
- Dropdowns (`<select>`, `<option>`, `<optgroup>`)
- Checkboxes & Radio Buttons
- Buttons (`<button>`, `submit`, `reset`)
- Fieldset & Legend (`<fieldset>`, `<legend>`)
- Form Validation (`required`, `pattern`, etc.)
- `<label>`, `<datalist>`, `<output>`

---

## 10. Semantic HTML

- What is Semantic HTML?
- `<header>`, `<footer>`, `<main>`, `<nav>`
- `<article>`, `<section>`, `<aside>`
- `<figure>`, `<figcaption>`, `<time>`
- `<mark>`, `<summary>`, `<details>`, `<progress>`

---

## 11. Multimedia (Audio & Video)

- `<audio>` and `<video>` Tags
- Supported File Types
- Controls: Autoplay, Loop, Mute
- Embedding with `<iframe>` (YouTube, Vimeo)
- `<track>` for Subtitles
- `<object>`, `<embed>`, `<param>`

---

## 12. HTML5 APIs

- Geolocation API
- Web Storage (localStorage, sessionStorage)
- Drag & Drop API
- Web Workers
- Canvas API (`<canvas>`)
- SVG Graphics (`<svg>`)
- WebSockets (Introduction)

---

## 13. Meta Tags & SEO Basics

- `<meta charset="UTF-8">`
- Viewport Meta Tag (`<meta name="viewport">`)
- Title and Description (`<title>`, `<meta name="description">`)
- Robots Tag (`<meta name="robots">`)
- Open Graph Tags (Facebook, Twitter)
- Canonical URL
- Favicons

---

## 14. Accessibility (A11y) in HTML

- Importance of Accessibility
- Semantic Elements for Screen Readers
- ARIA Roles (e.g., `role="navigation"`)
- Alt Text for Images
- Keyboard Navigation (`tabindex`, `accesskey`)
- Focus Management
- Color Contrast

---

## 15. Global Attributes

- `id`, `class`, `style`, `title`
- `lang`, `dir`
- `hidden`, `contenteditable`, `spellcheck`
- `tabindex`, `accesskey`
- `data-*` Custom Attributes

---

## 16. Advanced HTML Concepts

- HTML Entities (`&copy;`, `&lt;`, `&nbsp;`, etc.)
- Microdata & Structured Data (Schema.org)
- Web Components
  - Custom Elements
  - Shadow DOM
  - `<template>`, `<slot>`
- `<picture>` Element for Responsive Images
- Embedding Maps (Google Maps, OpenStreetMap)
- Progressive Enhancement Principles

---

## 17. Tools & Performance Optimization

- HTML Validators (W3C Validator)
- DevTools for Debugging
- VSCode Extensions (Live Server, Emmet, Prettier)
- Linting Tools (HTMLHint)
- Lazy Loading Assets
- `<noscript>` Fallback
- Security Best Practices (CSP, escaping, no inline scripts)

---

## 18. Final Project: Build a Complete Website

You’ll build multiple mini-projects and combine them into a responsive, semantic, accessible HTML website.

### 🔨 Projects:
- Personal Portfolio Site
- Blog Layout
- E-Commerce Product Page
- Contact Form with Validation
- Responsive Design using `<picture>`
- Host with GitHub Pages / Netlify

---

## ✅ Congratulations!

You’ve completed the HTML Master-Level curriculum. Next steps:

- Learn CSS and JavaScript
- Build real projects
- Practice accessibility and performance
- Stay updated with new HTML specs!

